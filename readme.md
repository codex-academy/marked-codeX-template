# remark slides codeX template

Easily create slides using markdown with [remark](https://remarkjs.com/#1)

Open `slides.html` to see the slideshow. Use the `forward` and `back` arrows to move through the slideshow.
